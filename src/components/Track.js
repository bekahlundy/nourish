import React from 'react';

const Track = () => {
  return (
    <div>
      <p>track here</p>
      <input />
      <button>track</button>

      {/* ask about different preset items and how to set it up (components?)(show the strides app) */}
    </div>
  )
}

export default Track;
